# UI-talad-008 · รายละเอียดบิล

> ไฟล์นี้ generate จาก `wireframes/UI-talad-008.json` — **ห้ามแก้ด้วยมือ** แก้ที่ต้นทางแล้วสร้างใหม่ด้วย `/mock:wireframe` (ประตู 23)
> ใบนี้คือ **wireframe L1 ที่คนอ่าน** — โครงของหน้า ไม่มีสี ไม่มีขนาด · ภาพที่มีธีมอยู่ที่ `/mock:html` (L2) · ใบคำขอถึงเครื่องมือออกแบบอยู่ที่ `/mock:handoff --prompt`

## 1 · บริบทของหน้า

- **หน้าจอ:** `UI-talad-008`
- **จุดประสงค์:** ดูรายการ ส่วนลด ผู้ขาย สมาชิก และสถานะของบิล พิมพ์ใบเสร็จซ้ำ และให้เจ้าของร้านยกเลิกบิลของวันนี้
- **ผู้ใช้ที่เห็นหน้านี้:** `ROLE-001` · `ROLE-002`
- **ชนิดของหน้า:** `form` · **ระดับที่มีอยู่แล้ว:** `L1`
- **ภาพนี้:** `MCK-talad-009` · ชนิดของหน้าที่ design ประกาศ: `detail` · ไฟล์ต้นทาง: `wireframes/UI-talad-008.json`

## 2 · สิ่งที่ต้องมี

_โซนเรียงตามลำดับที่หน้าถูกวาด · `วางโดย` บอกว่าใครเป็นคนจัดโซนนี้ — `design` คือผู้ประกาศ `zones[]` (ประตู 47) ส่วน `mock` คือโซนที่ตัวเรนเดอร์เพิ่มให้เอง_

### โซน `header` (header) — วางโดย `mock`

| ชนิด | ข้อความ | ปลายทาง | `data-testid` |
|---|---|---|---|
| title | รายละเอียดบิล | — | — ยังไม่มีใครตั้งชื่อ |

### โซน `header` (form) — วางโดย `design`

| ชนิด | ข้อความ | ปลายทาง | `data-testid` |
|---|---|---|---|
| field | เลขใบเสร็จ | — | `ui-talad-008-ent-011-receipt-no` |
| field | วันเวลา | — | `ui-talad-008-ent-011-paid-at` |
| field | ผู้ขาย | — | `ui-talad-008-ent-011-seller` |
| field | สมาชิก | — | `ui-talad-008-ent-011-member` |
| field | สถานะ | — | `ui-talad-008-ent-011-status` |
| action | กลับ | → หน้าประวัติการขาย | `ui-talad-008-back` |

### โซน `lines` (form) — วางโดย `design`

| ชนิด | ข้อความ | ปลายทาง | `data-testid` |
|---|---|---|---|
| field | สินค้า | — | `ui-talad-008-ent-012-product` |
| field | จำนวน | — | `ui-talad-008-ent-012-qty` |
| field | ราคา ณ ตอนขาย | — | `ui-talad-008-ent-012-unit-price` |
| field | โปรของบรรทัด | — | `ui-talad-008-ent-012-promotion-version` |
| field | ส่วนลดของบรรทัด | — | `ui-talad-008-ent-012-promo-discount` |

### โซน `totals` (form) — วางโดย `design`

| ชนิด | ข้อความ | ปลายทาง | `data-testid` |
|---|---|---|---|
| field | ส่วนลดโปรโมชั่น | — | `ui-talad-008-ent-011-promo-discount-total` |
| field | ส่วนลดทั้งบิล | — | `ui-talad-008-ent-011-bill-discount` |
| field | ส่วนลดสมาชิก | — | `ui-talad-008-ent-011-member-discount` |
| field | ยอดชำระ | — | `ui-talad-008-ent-011-net-total` |
| action | พิมพ์ใบเสร็จซ้ำ | → อยู่ที่หน้าเดิม | `ui-talad-008-reprint` |
| action | ยกเลิกบิล | → หน้าต่างยกเลิกบิล | `ui-talad-008-void` |

### โซน `void-info` (form) — วางโดย `design`

| ชนิด | ข้อความ | ปลายทาง | `data-testid` |
|---|---|---|---|
| field | ผู้ยกเลิก | — | `ui-talad-008-ent-011-voided-by` |
| field | เวลาที่ยกเลิก | — | `ui-talad-008-ent-011-voided-at` |
| field | เหตุผลการยกเลิก | — | `ui-talad-008-ent-011-void-reason` |

## 3 · ห้าสถานะ

| สถานะ | เกิดกับหน้านี้ไหม | ข้อความที่ต้องแสดง |
|---|---|---|
| `empty` | ไม่เกิด | ไม่เกิด — หน้าเปิดจากบิลที่มีอยู่เสมอ |
| `loading` | เกิด | แสดงโครงบิลระหว่างโหลด |
| `error` | เกิด | โหลดข้อมูลไม่สำเร็จ แสดงข้อความ "โหลดข้อมูลไม่สำเร็จ" พร้อมปุ่มลองใหม่ ข้อมูลที่กรอกค้างไว้ไม่หาย |
| `unauthorized` | เกิด | พนักงานขายเปิดบิลของคนอื่นตรง ๆ: API ปฏิเสธและพากลับประวัติการขายพร้อม "คุณไม่มีสิทธิ์เข้าถึงหน้านี้" ไม่แสดงข้อมูลบิล (BR-talad-018@v1) · ยังไม่ล็อกอิน: พาไปหน้าเข้าสู่ระบบ |
| `overflow` | เกิด | บิลหลายบรรทัด: ส่วนรายการเลื่อนได้ ยอดรวมอยู่กับที่ · เหตุผลการยกเลิกยาวตัดบรรทัด |

_ข้อความในตารางนี้คัดมาคำต่อคำจากที่ design ประกาศไว้ **ห้ามเขียนใหม่ให้สวยขึ้น**_

## 4 · กติกา UI ที่บังคับ

- **UIC-001** — ปุ่มประจำแถวอยู่คอลัมน์แรกของแถว เรียงลำดับตายตัว: view · edit · delete · manage · command
  - ใช้กับ: ทุกหน้าที่แสดงเป็นรายการแถว — ปุ่มประจำแถวทุกปุ่ม (ชนิด view · edit · delete · manage · command — ชนิดเป็นของ design ตั้งแต่ประตู 35)
- **UIC-002** — แสดงเป็นไอคอน ไม่ใช่ปุ่มตัวหนังสือ และใช้ชื่อ action ที่ design ประกาศไว้ใน actions[].name เป็นชื่อสำหรับ screen reader
  - ใช้กับ: ปุ่มประจำแถวทุกปุ่ม
- **UIC-003** — เปิดเป็น modal ทับรายการ ไม่พาไปหน้ารายละเอียดแยก
  - ใช้กับ: ปุ่มประจำแถวชนิด view

