# UI-talad-012 · รายละเอียดสินค้า

> ไฟล์นี้ generate จาก `wireframes/UI-talad-012.json` — **ห้ามแก้ด้วยมือ** แก้ที่ต้นทางแล้วสร้างใหม่ด้วย `/mock:wireframe` (ประตู 23)
> ใบนี้คือ **wireframe L1 ที่คนอ่าน** — โครงของหน้า ไม่มีสี ไม่มีขนาด · ภาพที่มีธีมอยู่ที่ `/mock:html` (L2) · ใบคำขอถึงเครื่องมือออกแบบอยู่ที่ `/mock:handoff --prompt`

## 1 · บริบทของหน้า

- **หน้าจอ:** `UI-talad-012`
- **จุดประสงค์:** ดูข้อมูลสินค้า ประวัติราคา และรายการปรับสต็อก และเริ่มแก้ราคา ปรับสต็อก หรือเลิกขาย
- **ผู้ใช้ที่เห็นหน้านี้:** `ROLE-002`
- **ชนิดของหน้า:** `form` · **ระดับที่มีอยู่แล้ว:** `L1`
- **ภาพนี้:** `MCK-talad-013` · ชนิดของหน้าที่ design ประกาศ: `detail` · ไฟล์ต้นทาง: `wireframes/UI-talad-012.json`

## 2 · สิ่งที่ต้องมี

_โซนเรียงตามลำดับที่หน้าถูกวาด · `วางโดย` บอกว่าใครเป็นคนจัดโซนนี้ — `design` คือผู้ประกาศ `zones[]` (ประตู 47) ส่วน `mock` คือโซนที่ตัวเรนเดอร์เพิ่มให้เอง_

### โซน `header` (header) — วางโดย `mock`

| ชนิด | ข้อความ | ปลายทาง | `data-testid` |
|---|---|---|---|
| title | รายละเอียดสินค้า | — | — ยังไม่มีใครตั้งชื่อ |

### โซน `summary` (form) — วางโดย `design`

| ชนิด | ข้อความ | ปลายทาง | `data-testid` |
|---|---|---|---|
| field | ชื่อสินค้า | — | `ui-talad-012-ent-001-name` |
| field | คงเหลือ | — | `ui-talad-012-ent-001-stock-qty` |
| field | จุดเตือน | — | `ui-talad-012-ent-001-low-stock-threshold` |
| field | ราคาปัจจุบัน | — | `ui-talad-012-ent-002-price` |
| action | แก้ไขข้อมูล | → หน้าเพิ่ม/แก้ไขสินค้า | `ui-talad-012-edit` |
| action | แก้ราคา | → หน้าต่างแก้ราคาสินค้า | `ui-talad-012-edit-price` |
| action | ปรับสต็อก | → หน้าต่างปรับสต็อก | `ui-talad-012-adjust-stock` |
| action | กลับ | → หน้าสต็อก | `ui-talad-012-back` |

### โซน `price-history` (form) — วางโดย `design`

| ชนิด | ข้อความ | ปลายทาง | `data-testid` |
|---|---|---|---|
| field | ราคาเดิม | — | `ui-talad-012-ent-002-previous-price` |
| field | ผู้แก้ราคา | — | `ui-talad-012-ent-002-changed-by` |
| field | เวลาที่แก้ราคา | — | `ui-talad-012-ent-002-changed-at` |

### โซน `adjustments` (form) — วางโดย `design`

| ชนิด | ข้อความ | ปลายทาง | `data-testid` |
|---|---|---|---|
| field | จำนวนที่ปรับ | — | `ui-talad-012-ent-003-quantity-delta` |
| field | เหตุผล | — | `ui-talad-012-ent-003-reason` |
| field | ผู้ปรับ | — | `ui-talad-012-ent-003-adjusted-by` |
| field | เวลาที่ปรับ | — | `ui-talad-012-ent-003-adjusted-at` |
| field | หมายเหตุ | — | `ui-talad-012-ent-003-note` |

### โซน `danger` (toolbar) — วางโดย `design`

| ชนิด | ข้อความ | ปลายทาง | `data-testid` |
|---|---|---|---|
| action | ลบสินค้า | → หน้าสต็อก | `ui-talad-012-discontinue` |

## 3 · ห้าสถานะ

| สถานะ | เกิดกับหน้านี้ไหม | ข้อความที่ต้องแสดง |
|---|---|---|
| `empty` | เกิด | ยังไม่มีประวัติราคาหรือรายการปรับสต็อก: แสดง "ยังไม่มีรายการ" ในส่วนนั้น |
| `loading` | เกิด | แสดงโครงแต่ละส่วนระหว่างโหลด |
| `error` | เกิด | โหลดข้อมูลไม่สำเร็จ แสดงข้อความ "โหลดข้อมูลไม่สำเร็จ" พร้อมปุ่มลองใหม่ ข้อมูลที่กรอกค้างไว้ไม่หาย |
| `unauthorized` | เกิด | พนักงานขายเปิดหน้านี้ตรง ๆ ถูกพากลับหน้าขายและแสดง "คุณไม่มีสิทธิ์เข้าถึงหน้านี้" โดยไม่แสดงข้อมูลใดของหน้านี้ (BR-talad-018@v1) · ยังไม่ล็อกอิน: พาไปหน้าเข้าสู่ระบบ |
| `overflow` | เกิด | ประวัติราคาและรายการปรับสต็อกยาว: แต่ละส่วนแบ่งหน้าจากเซิร์ฟเวอร์ หน้าละ 20 แถว |

_ข้อความในตารางนี้คัดมาคำต่อคำจากที่ design ประกาศไว้ **ห้ามเขียนใหม่ให้สวยขึ้น**_

## 4 · กติกา UI ที่บังคับ

- **UIC-001** — ปุ่มประจำแถวอยู่คอลัมน์แรกของแถว เรียงลำดับตายตัว: view · edit · delete · manage · command
  - ใช้กับ: ทุกหน้าที่แสดงเป็นรายการแถว — ปุ่มประจำแถวทุกปุ่ม (ชนิด view · edit · delete · manage · command — ชนิดเป็นของ design ตั้งแต่ประตู 35)
- **UIC-002** — แสดงเป็นไอคอน ไม่ใช่ปุ่มตัวหนังสือ และใช้ชื่อ action ที่ design ประกาศไว้ใน actions[].name เป็นชื่อสำหรับ screen reader
  - ใช้กับ: ปุ่มประจำแถวทุกปุ่ม
- **UIC-003** — เปิดเป็น modal ทับรายการ ไม่พาไปหน้ารายละเอียดแยก
  - ใช้กับ: ปุ่มประจำแถวชนิด view

