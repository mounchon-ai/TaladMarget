# RPT-talad-001 · ใบเสร็จ

> ไฟล์นี้ generate จาก `wireframes/RPT-talad-001.json` — **ห้ามแก้ด้วยมือ** แก้ที่ต้นทางแล้วสร้างใหม่ด้วย `/mock:wireframe` (ประตู 23)
> ใบนี้คือ **wireframe L1 ที่คนอ่าน** — โครงของหน้า ไม่มีสี ไม่มีขนาด · ภาพที่มีธีมอยู่ที่ `/mock:html` (L2) · ใบคำขอถึงเครื่องมือออกแบบอยู่ที่ `/mock:handoff --prompt`

## 1 · บริบทของหน้า

- **หน้าจอ:** `RPT-talad-001`
- **จุดประสงค์:** แสดงใบเสร็จของบิลเต็มจอหลังชำระ และพิมพ์ให้ลูกค้าเมื่อต้องการ
- **ผู้ใช้ที่เห็นหน้านี้:** `ROLE-001` · `ROLE-002`
- **ชนิดของหน้า:** `report` · **ระดับที่มีอยู่แล้ว:** `L1`
- **ภาพนี้:** `MCK-talad-004` · ชนิดของหน้าที่ design ประกาศ: `report` · ไฟล์ต้นทาง: `wireframes/RPT-talad-001.json`

## 2 · สิ่งที่ต้องมี

_โซนเรียงตามลำดับที่หน้าถูกวาด · `วางโดย` บอกว่าใครเป็นคนจัดโซนนี้ — `design` คือผู้ประกาศ `zones[]` (ประตู 47) ส่วน `mock` คือโซนที่ตัวเรนเดอร์เพิ่มให้เอง_

### โซน `header` (header) — วางโดย `mock`

| ชนิด | ข้อความ | ปลายทาง | `data-testid` |
|---|---|---|---|
| title | ใบเสร็จ | — | — ยังไม่มีใครตั้งชื่อ |

### โซน `header` (columns) — วางโดย `design`

| ชนิด | ข้อความ | ปลายทาง | `data-testid` |
|---|---|---|---|
| field | เลขใบเสร็จ | — | `rpt-talad-001-ent-011-receipt-no` |
| field | วันเวลา | — | `rpt-talad-001-ent-011-paid-at` |
| field | ผู้ขาย | — | `rpt-talad-001-ent-011-seller` |
| field | สมาชิก | — | `rpt-talad-001-ent-011-member` |

### โซน `lines` (columns) — วางโดย `design`

| ชนิด | ข้อความ | ปลายทาง | `data-testid` |
|---|---|---|---|
| field | สินค้า | — | `rpt-talad-001-ent-012-product` |
| field | จำนวน | — | `rpt-talad-001-ent-012-qty` |
| field | ราคา | — | `rpt-talad-001-ent-012-unit-price` |
| field | โปรของบรรทัด | — | `rpt-talad-001-ent-012-promotion-version` |
| field | ส่วนลดของบรรทัด | — | `rpt-talad-001-ent-012-promo-discount` |

### โซน `totals` (columns) — วางโดย `design`

| ชนิด | ข้อความ | ปลายทาง | `data-testid` |
|---|---|---|---|
| field | ส่วนลดโปรโมชั่น | — | `rpt-talad-001-ent-011-promo-discount-total` |
| field | ส่วนลดทั้งบิล | — | `rpt-talad-001-ent-011-bill-discount` |
| field | ส่วนลดสมาชิก | — | `rpt-talad-001-ent-011-member-discount` |
| field | ยอดชำระ | — | `rpt-talad-001-ent-011-net-total` |
| action | พิมพ์ | → อยู่ที่หน้าเดิม | `rpt-talad-001-print` |
| action | ปิด | → หน้าขายพร้อมตะกร้าว่าง | `rpt-talad-001-close` |

## 3 · ห้าสถานะ

| สถานะ | เกิดกับหน้านี้ไหม | ข้อความที่ต้องแสดง |
|---|---|---|
| `empty` | ไม่เกิด | ไม่เกิด — ใบเสร็จเกิดจากบิลที่มีอย่างน้อยหนึ่งบรรทัดเสมอ |
| `loading` | ไม่เกิด | ไม่เกิด — ใบเสร็จแสดงจากผลการชำระที่เพิ่งได้รับ |
| `error` | เกิด | หน้าต่างพิมพ์เปิดไม่ได้หรือถูกยกเลิก: บิลบันทึกแล้วไม่เปลี่ยน พิมพ์ซ้ำได้จากประวัติการขาย |
| `unauthorized` | ไม่เกิด | ไม่เกิดสำหรับผู้ที่ล็อกอินแล้ว — ทั้งพนักงานขายและเจ้าของร้านเปิดได้ · ยังไม่ล็อกอิน: พาไปหน้าเข้าสู่ระบบแล้วกลับมาหน้านี้หลังล็อกอิน (BR-talad-005@v1) |
| `overflow` | เกิด | บิลหลายบรรทัด: ใบเสร็จเลื่อนบนจอ และพิมพ์ต่อเนื่องหลายหน้า |

_ข้อความในตารางนี้คัดมาคำต่อคำจากที่ design ประกาศไว้ **ห้ามเขียนใหม่ให้สวยขึ้น**_

## 4 · กติกา UI ที่บังคับ

- **UIC-001** — ปุ่มประจำแถวอยู่คอลัมน์แรกของแถว เรียงลำดับตายตัว: view · edit · delete · manage · command
  - ใช้กับ: ทุกหน้าที่แสดงเป็นรายการแถว — ปุ่มประจำแถวทุกปุ่ม (ชนิด view · edit · delete · manage · command — ชนิดเป็นของ design ตั้งแต่ประตู 35)
- **UIC-002** — แสดงเป็นไอคอน ไม่ใช่ปุ่มตัวหนังสือ และใช้ชื่อ action ที่ design ประกาศไว้ใน actions[].name เป็นชื่อสำหรับ screen reader
  - ใช้กับ: ปุ่มประจำแถวทุกปุ่ม
- **UIC-003** — เปิดเป็น modal ทับรายการ ไม่พาไปหน้ารายละเอียดแยก
  - ใช้กับ: ปุ่มประจำแถวชนิด view

