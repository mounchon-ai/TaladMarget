# UI-talad-010 · สต็อก

> ไฟล์นี้ generate จาก `wireframes/UI-talad-010.json` — **ห้ามแก้ด้วยมือ** แก้ที่ต้นทางแล้วสร้างใหม่ด้วย `/mock:wireframe` (ประตู 23)
> ใบนี้คือ **wireframe L1 ที่คนอ่าน** — โครงของหน้า ไม่มีสี ไม่มีขนาด · ภาพที่มีธีมอยู่ที่ `/mock:html` (L2) · ใบคำขอถึงเครื่องมือออกแบบอยู่ที่ `/mock:handoff --prompt`

## 1 · บริบทของหน้า

- **หน้าจอ:** `UI-talad-010`
- **จุดประสงค์:** ดูสินค้าที่ยังขายอยู่ จำนวนคงเหลือ และป้ายใกล้หมด เพื่อเปิดจัดการหรือเพิ่มสินค้า
- **ผู้ใช้ที่เห็นหน้านี้:** `ROLE-002`
- **ชนิดของหน้า:** `rows` · **ระดับที่มีอยู่แล้ว:** `L1`
- **ภาพนี้:** `MCK-talad-011` · ชนิดของหน้าที่ design ประกาศ: `list` · ไฟล์ต้นทาง: `wireframes/UI-talad-010.json`

## 2 · สิ่งที่ต้องมี

_โซนเรียงตามลำดับที่หน้าถูกวาด · `วางโดย` บอกว่าใครเป็นคนจัดโซนนี้ — `design` คือผู้ประกาศ `zones[]` (ประตู 47) ส่วน `mock` คือโซนที่ตัวเรนเดอร์เพิ่มให้เอง_

### โซน `header` (header) — วางโดย `mock`

| ชนิด | ข้อความ | ปลายทาง | `data-testid` |
|---|---|---|---|
| title | สต็อก | — | — ยังไม่มีใครตั้งชื่อ |

### โซน `filters` (form) — วางโดย `design`

| ชนิด | ข้อความ | ปลายทาง | `data-testid` |
|---|---|---|---|
| field | ค้นหา | — | `ui-talad-010-search-text` |
| action | ค้นหา | → อยู่ที่หน้าเดิม | `ui-talad-010-search` |
| action | เพิ่มสินค้า | → หน้าเพิ่ม/แก้ไขสินค้า | `ui-talad-010-add-product` |
| action | Export Excel | → อยู่ที่หน้าเดิม | `ui-talad-010-export` |

### โซน `results` (columns) — วางโดย `design`

| ชนิด | ข้อความ | ปลายทาง | `data-testid` |
|---|---|---|---|
| field | ชื่อสินค้า | — | `ui-talad-010-ent-001-name` |
| field | บาร์โค้ด | — | `ui-talad-010-ent-001-barcode` |
| field | ราคา | — | `ui-talad-010-ent-002-price` |
| field | คงเหลือ | — | `ui-talad-010-ent-001-stock-qty` |
| field | ป้ายใกล้หมด | — | `ui-talad-010-ent-001-low-stock-threshold` |
| action | ดูสินค้า | → หน้ารายละเอียดสินค้า | `ui-talad-010-open-product` |

## 3 · ห้าสถานะ

| สถานะ | เกิดกับหน้านี้ไหม | ข้อความที่ต้องแสดง |
|---|---|---|
| `empty` | เกิด | ยังไม่มีสินค้าหรือค้นหาไม่พบ: แสดง "ไม่พบสินค้า" พร้อมปุ่มเพิ่มสินค้า |
| `loading` | เกิด | ตารางแสดงโครงแถวระหว่างโหลด ปุ่มค้นหาถูกปิดจนผลกลับมา · ไม่เกิน 2 วินาทีตามเป้าของ NFR |
| `error` | เกิด | โหลดข้อมูลไม่สำเร็จ แสดงข้อความ "โหลดข้อมูลไม่สำเร็จ" พร้อมปุ่มลองใหม่ ข้อมูลที่กรอกค้างไว้ไม่หาย |
| `unauthorized` | เกิด | พนักงานขายเปิดหน้านี้ตรง ๆ ถูกพากลับหน้าขายและแสดง "คุณไม่มีสิทธิ์เข้าถึงหน้านี้" โดยไม่แสดงข้อมูลใดของหน้านี้ (BR-talad-018@v1) · ยังไม่ล็อกอิน: พาไปหน้าเข้าสู่ระบบ |
| `overflow` | เกิด | ข้อมูลเกินหนึ่งหน้า: แบ่งหน้าจากเซิร์ฟเวอร์ หน้าละ 20 แถว · ข้อความยาวตัดด้วย … และแสดงเต็มเมื่อชี้ |

_ข้อความในตารางนี้คัดมาคำต่อคำจากที่ design ประกาศไว้ **ห้ามเขียนใหม่ให้สวยขึ้น**_

## 4 · กติกา UI ที่บังคับ

- **UIC-001** — ปุ่มประจำแถวอยู่คอลัมน์แรกของแถว เรียงลำดับตายตัว: view · edit · delete · manage · command
  - ใช้กับ: ทุกหน้าที่แสดงเป็นรายการแถว — ปุ่มประจำแถวทุกปุ่ม (ชนิด view · edit · delete · manage · command — ชนิดเป็นของ design ตั้งแต่ประตู 35)
- **UIC-002** — แสดงเป็นไอคอน ไม่ใช่ปุ่มตัวหนังสือ และใช้ชื่อ action ที่ design ประกาศไว้ใน actions[].name เป็นชื่อสำหรับ screen reader
  - ใช้กับ: ปุ่มประจำแถวทุกปุ่ม
- **UIC-003** — เปิดเป็น modal ทับรายการ ไม่พาไปหน้ารายละเอียดแยก
  - ใช้กับ: ปุ่มประจำแถวชนิด view

