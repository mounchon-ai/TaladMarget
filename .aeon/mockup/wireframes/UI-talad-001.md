# UI-talad-001 · เข้าสู่ระบบ

> ไฟล์นี้ generate จาก `wireframes/UI-talad-001.json` — **ห้ามแก้ด้วยมือ** แก้ที่ต้นทางแล้วสร้างใหม่ด้วย `/mock:wireframe` (ประตู 23)
> ใบนี้คือ **wireframe L1 ที่คนอ่าน** — โครงของหน้า ไม่มีสี ไม่มีขนาด · ภาพที่มีธีมอยู่ที่ `/mock:html` (L2) · ใบคำขอถึงเครื่องมือออกแบบอยู่ที่ `/mock:handoff --prompt`

## 1 · บริบทของหน้า

- **หน้าจอ:** `UI-talad-001`
- **จุดประสงค์:** ผู้ใช้ยืนยันตัวตนด้วยชื่อผู้ใช้และรหัสผ่านเพื่อเข้าใช้งานหน้าขาย
- **ผู้ใช้ที่เห็นหน้านี้:** `ROLE-003`
- **ชนิดของหน้า:** `form` · **ระดับที่มีอยู่แล้ว:** `L1`
- **ภาพนี้:** `MCK-talad-001` · ชนิดของหน้าที่ design ประกาศ: `auth` · ไฟล์ต้นทาง: `wireframes/UI-talad-001.json`

## 2 · สิ่งที่ต้องมี

_โซนเรียงตามลำดับที่หน้าถูกวาด · `วางโดย` บอกว่าใครเป็นคนจัดโซนนี้ — `design` คือผู้ประกาศ `zones[]` (ประตู 47) ส่วน `mock` คือโซนที่ตัวเรนเดอร์เพิ่มให้เอง_

### โซน `header` (header) — วางโดย `mock`

| ชนิด | ข้อความ | ปลายทาง | `data-testid` |
|---|---|---|---|
| title | เข้าสู่ระบบ | — | — ยังไม่มีใครตั้งชื่อ |

### โซน `sign-in-form` (form) — วางโดย `design`

| ชนิด | ข้อความ | ปลายทาง | `data-testid` |
|---|---|---|---|
| field | ชื่อผู้ใช้ | — | `ui-talad-001-ent-008-username` |
| field | รหัสผ่าน | — | `ui-talad-001-password` |
| action | เข้าสู่ระบบ | → หน้าขาย หรือหน้าที่ผู้ใช้ตั้งใจเปิดก่อนถูกพามาหน้านี้ | `ui-talad-001-sign-in` |

## 3 · ห้าสถานะ

| สถานะ | เกิดกับหน้านี้ไหม | ข้อความที่ต้องแสดง |
|---|---|---|
| `empty` | ไม่เกิด | ไม่เกิด — หน้านี้เป็นฟอร์มสองช่อง ไม่แสดงรายการข้อมูล |
| `loading` | เกิด | ปุ่มเข้าสู่ระบบถูกปิดและแสดงตัวหมุนระหว่างตรวจสอบ |
| `error` | เกิด | แสดง "ไม่พบชื่อผู้ใช้" หรือ "รหัสผ่านไม่ถูกต้อง" ใต้ฟอร์มตาม BR-talad-005@v1 และยังอยู่ที่หน้านี้ · บัญชีที่ปิดใช้งาน: ไม่ให้เข้า (ข้อความรอกำหนดที่ req) · เชื่อมต่อเซิร์ฟเวอร์ไม่ได้: แจ้งให้ลองใหม่ |
| `unauthorized` | ไม่เกิด | ไม่เกิด — หน้านี้เปิดได้โดยไม่ต้องล็อกอิน · ผู้ที่ล็อกอินอยู่แล้วถูกพาไปหน้าขาย |
| `overflow` | ไม่เกิด | ไม่เกิด — ฟอร์มสองช่อง ไม่มีรายการ |

_ข้อความในตารางนี้คัดมาคำต่อคำจากที่ design ประกาศไว้ **ห้ามเขียนใหม่ให้สวยขึ้น**_

## 4 · กติกา UI ที่บังคับ

- **UIC-001** — ปุ่มประจำแถวอยู่คอลัมน์แรกของแถว เรียงลำดับตายตัว: view · edit · delete · manage · command
  - ใช้กับ: ทุกหน้าที่แสดงเป็นรายการแถว — ปุ่มประจำแถวทุกปุ่ม (ชนิด view · edit · delete · manage · command — ชนิดเป็นของ design ตั้งแต่ประตู 35)
- **UIC-002** — แสดงเป็นไอคอน ไม่ใช่ปุ่มตัวหนังสือ และใช้ชื่อ action ที่ design ประกาศไว้ใน actions[].name เป็นชื่อสำหรับ screen reader
  - ใช้กับ: ปุ่มประจำแถวทุกปุ่ม
- **UIC-003** — เปิดเป็น modal ทับรายการ ไม่พาไปหน้ารายละเอียดแยก
  - ใช้กับ: ปุ่มประจำแถวชนิด view

