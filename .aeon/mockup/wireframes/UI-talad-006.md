# UI-talad-006 · ข้อมูลสมาชิก

> ไฟล์นี้ generate จาก `wireframes/UI-talad-006.json` — **ห้ามแก้ด้วยมือ** แก้ที่ต้นทางแล้วสร้างใหม่ด้วย `/mock:wireframe` (ประตู 23)
> ใบนี้คือ **wireframe L1 ที่คนอ่าน** — โครงของหน้า ไม่มีสี ไม่มีขนาด · ภาพที่มีธีมอยู่ที่ `/mock:html` (L2) · ใบคำขอถึงเครื่องมือออกแบบอยู่ที่ `/mock:handoff --prompt`

## 1 · บริบทของหน้า

- **หน้าจอ:** `UI-talad-006`
- **จุดประสงค์:** ดูและแก้ชื่อหรือเบอร์โทรของสมาชิก และให้เจ้าของร้านซ่อนสมาชิก
- **ผู้ใช้ที่เห็นหน้านี้:** `ROLE-001` · `ROLE-002`
- **ชนิดของหน้า:** `form` · **ระดับที่มีอยู่แล้ว:** `L1`
- **ภาพนี้:** `MCK-talad-007` · ชนิดของหน้าที่ design ประกาศ: `edit` · ไฟล์ต้นทาง: `wireframes/UI-talad-006.json`

## 2 · สิ่งที่ต้องมี

_โซนเรียงตามลำดับที่หน้าถูกวาด · `วางโดย` บอกว่าใครเป็นคนจัดโซนนี้ — `design` คือผู้ประกาศ `zones[]` (ประตู 47) ส่วน `mock` คือโซนที่ตัวเรนเดอร์เพิ่มให้เอง_

### โซน `header` (header) — วางโดย `mock`

| ชนิด | ข้อความ | ปลายทาง | `data-testid` |
|---|---|---|---|
| title | ข้อมูลสมาชิก | — | — ยังไม่มีใครตั้งชื่อ |

### โซน `form` (form) — วางโดย `design`

| ชนิด | ข้อความ | ปลายทาง | `data-testid` |
|---|---|---|---|
| field | ชื่อ | — | `ui-talad-006-ent-004-name` |
| field | เบอร์โทร | — | `ui-talad-006-ent-004-phone` |
| field | ยอดซื้อสะสม | — | `ui-talad-006-ent-004-accumulated-amount` |
| action | บันทึก | → อยู่ที่หน้าเดิม | `ui-talad-006-save` |
| action | กลับ | → หน้าสมาชิก | `ui-talad-006-back` |

### โซน `danger` (toolbar) — วางโดย `design`

| ชนิด | ข้อความ | ปลายทาง | `data-testid` |
|---|---|---|---|
| action | ลบสมาชิก | → หน้าสมาชิก | `ui-talad-006-hide-member` |

## 3 · ห้าสถานะ

| สถานะ | เกิดกับหน้านี้ไหม | ข้อความที่ต้องแสดง |
|---|---|---|
| `empty` | ไม่เกิด | ไม่เกิด — หน้าเปิดจากสมาชิกที่มีอยู่เสมอ |
| `loading` | เกิด | แสดงโครงฟอร์มระหว่างโหลด · ปุ่มบันทึกถูกปิดระหว่างบันทึก |
| `error` | เกิด | บันทึกไม่ผ่าน: แสดงข้อความใต้ช่องที่ผิด ค่าเดิมในระบบไม่เปลี่ยน · สมาชิกถูกซ่อนไปแล้วระหว่างเปิดหน้า: แจ้ง "ไม่พบสมาชิก" และกลับรายการ |
| `unauthorized` | ไม่เกิด | ไม่เกิดสำหรับผู้ที่ล็อกอินแล้ว — ทั้งสองบทบาทเปิดได้ แต่พนักงานขายไม่เห็นปุ่มลบสมาชิก และ API ปฏิเสธถ้าเรียกตรง (BR-talad-019@v1) · ยังไม่ล็อกอิน: พาไปหน้าเข้าสู่ระบบ |
| `overflow` | เกิด | ข้อความยาวเกินช่อง: ช่องเลื่อนภายในตัวเอง ไม่ดันรูปหน้า · ไม่มีรายการยาวบนหน้านี้ |

_ข้อความในตารางนี้คัดมาคำต่อคำจากที่ design ประกาศไว้ **ห้ามเขียนใหม่ให้สวยขึ้น**_

## 4 · กติกา UI ที่บังคับ

- **UIC-001** — ปุ่มประจำแถวอยู่คอลัมน์แรกของแถว เรียงลำดับตายตัว: view · edit · delete · manage · command
  - ใช้กับ: ทุกหน้าที่แสดงเป็นรายการแถว — ปุ่มประจำแถวทุกปุ่ม (ชนิด view · edit · delete · manage · command — ชนิดเป็นของ design ตั้งแต่ประตู 35)
- **UIC-002** — แสดงเป็นไอคอน ไม่ใช่ปุ่มตัวหนังสือ และใช้ชื่อ action ที่ design ประกาศไว้ใน actions[].name เป็นชื่อสำหรับ screen reader
  - ใช้กับ: ปุ่มประจำแถวทุกปุ่ม
- **UIC-003** — เปิดเป็น modal ทับรายการ ไม่พาไปหน้ารายละเอียดแยก
  - ใช้กับ: ปุ่มประจำแถวชนิด view

