// AC-talad-045 · shared by the server check (requireScreen) and the client notice, so it holds no server import.
export const DENIED_PARAM = "denied";
/** BR-talad-018@v1 — word for word. */
export const DENIED_MESSAGE = "คุณไม่มีสิทธิ์เข้าถึงหน้านี้";
